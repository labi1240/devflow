import type { NextPage } from "next";
import styles from "./gallery-card.module.css";

const GalleryCard: NextPage = () => {
  return <div className={styles.conditionChecker} />;
};

export default GalleryCard;
